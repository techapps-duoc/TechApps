instalar el tesseract.exe y cambiar la direccion segun corresponda revisar si 
genera la carpeta ya que tuve hacerla manual para que funcionara la parte
a cambiar es la sig. 

cambiar ruta en detectarPatente.py
pytesseract.pytesseract.tesseract_cmd = r'C:\ruta\a\tesseract.exe'

En cmd p
pip install requests

pip install pyserial    
en el ide revisa el puerto COM segun numero q corresponda entrada 
t cambia el  Python (arduino_control.py, main_entrada.py, o main_salida.py).


en terminal de visual 
pip install opencv-python
pip install numpy
pip install pytesseract
pip install -r requirements.txt



